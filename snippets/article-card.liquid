<div class="card w-96 bg-base-100 shadow-xl">
  <div class="card-body">
    <h2 class="card-title">
        <a href="{{ article.url }}" class="link">{{ article.title | escape }}</a>
        <div class="badge badge-neutral">{{ article.object_type }}</div>
    </h2>
    <p>If a dog chews shoes whose shoes does he choose?</p>
  </div>
</div>
